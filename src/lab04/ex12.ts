import { checkIfNumberIsPalindrome } from "../lab03/ex09a";

/**
 * 
 * @param value number to check
 * @returns returns true or false depending if the value is a palindrome or not
 */

export function checkIfNumberIsPalindromeLab04(value: number): boolean {

    let result: boolean = checkIfNumberIsPalindrome(value);

    return result;
}