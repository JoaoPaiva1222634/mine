/*
function getMatrixDeterminant(matrix: number[][]): number {

    let matrixDeterminant: number = 0;

    for (let i = 0; i != matrix.length)



        return matrixDeterminant
}

function getMinor(matrix: number[][], firstRowElement:): number[][] {

    let minor: number[][] = [];

    for (let i = 0; i != matrix.length; i++) {
        for (let j = 0; j != matrix[i].length; j++)
            if (i != 0 && j != i)
                minor[index1][index2] = matrix[i][j];

        minor[j][k] = 
    }
    return minor;
} 
*/